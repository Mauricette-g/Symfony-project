<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Stubborn Store{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
</head>

<body>
<header>
  <nav class="navbar navbar-expand-lg fixed-top navbar-light bg-light">
    <div class="container-fluid">
      <div class = "col"> 
        <a href="{{ path('home') }}" class="navbar-brand">
          <img src="{{ asset('images/Logo.png') }}" alt="logo stubborn" height="50">
        </a>
        <div class="text-muted small">Don't compromise on your look</div>
      </div>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" 
              aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>


      <div class="collapse navbar-collapse" id="navbarCollapse">
        <ul class="navbar-nav ms-auto"> {# ici on ajoute ms-auto pour aligner à droite #}
          {% if app.user %}
            <li class="nav-item">
              <a href="{{ path('home') }}" class="nav-link text-uppercase">Accueil</a>
            </li>
            <li class="nav-item">
              <a href="{{ path('products_list') }}" class="nav-link text-uppercase">Boutique</a>
            </li>
            <li class="nav-item">
              <a href="{{ path('cart_index') }}" class="nav-link text-uppercase">Panier</a>
            </li>
            <li class="nav-item">
              <a href="{{ path('app_logout') }}" class="nav-link text-uppercase">Se déconnecter</a>
            </li>
          {% else %}
            <li class="nav-item">
            <a href="{{ path('home') }}" class="nav-link text-uppercase">Accueil</a>
            </li>
            <li class="nav-item">
              <a href="{{ path('register_user') }}" class="nav-link text-uppercase">S'inscrire</a>
            </li>
            <li class="nav-item">
              <a href="{{ path('app_login') }}" class="nav-link text-uppercase">Se connecter</a>
            </li>
        {% endif %}
        </ul>
      </div>
    </div>
  </nav>
</header>

<main class="container" style="padding-top: 80px; padding-bottom: 60px;">
    {% block body %}{% endblock %}
</main>

<footer class="footer bg-light">
    <div class="container text-center py-3">
        <p class="mb-1">{{ '©' }} {{ "now"|date("Y") }} Stubborn Store - Tous droits réservés</p>
        <p class="mb-1">Piccadilly Circus, London W1J 0DA, Royaume-Uni</p>
        <p class="mb-0">Adresse de contact : {{ 'stubborn@blabla.com' }}</p>
    </div>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
</body>
</html>
